<h1><%= @list.name %></h1>

<% @movies.each do |movie| %>
  <p><%= movie.title %>  </p>
  <p><%= movie.overview %>  </p>
  <p><%= movie.rating %> </p>
  <p>" <%=  Bookmark.find_by_movie_id(movie.id).comment %></p>
<% end %>

<%= link_to 'Add a new movie', new_list_bookmark_path(@list) %>
